export type PositionConfig = {
  right: boolean;
  bottom: boolean;
};

export type InsertCSS = {
  css: string | null;
};

export type LoadURL = {
  url: string | null;
};

export type WindowSize = {
  width: number;
  height: number;
};
